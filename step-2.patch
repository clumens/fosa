diff --git a/configure.ac b/configure.ac
index 6b837407e..855a61d91 100644
--- a/configure.ac
+++ b/configure.ac
@@ -1969,6 +1969,9 @@ AS_IF([test $enable_fatal_warnings -ne $DISABLED], [
     AC_MSG_NOTICE([Enabling fatal compiler warnings])
     CFLAGS="$CFLAGS $WERROR"
 ])
+
+CFLAGS="$CFLAGS -fplugin=python -fplugin-arg-python-script=/home/clumens/src/fosa/check-args.py -fplugin-arg-python-messages=/home/clumens/src/pacemaker/fns.pickle"
+
 AC_SUBST(CFLAGS)
 
 dnl This is useful for use in Makefiles that need to remove one specific flag
